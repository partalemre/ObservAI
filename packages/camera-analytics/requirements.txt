opencv-python>=4.8.0
numpy>=1.24.0

# AI / ML
ultralytics>=8.3.0
# NOTE: lap is intentionally omitted - use lapx (works on Windows without C++ build tools)
lapx>=0.5.2
insightface>=0.7.3

# GPU-accelerated ONNX runtime for InsightFace demographics on NVIDIA GPU
# On Mac M-series: pip install onnxruntime-silicon
# On CPU-only:     pip install onnxruntime
onnxruntime-gpu>=1.19.0

# MiVOLO support
timm>=0.9.0
yacs>=0.1.8

# Streaming
python-socketio>=5.11.0
aiohttp>=3.9.0
yt-dlp>=2025.12.8
